const fruits = ["apple", "banana", "orange"];

fruits.forEach((fruit)=>{
    console.log(fruit);
})

/**
 * forEach is a higher order function which is used to iterate array and objects
 * It takes a callback function
 * It takes one callback fun and perform the operation
 */