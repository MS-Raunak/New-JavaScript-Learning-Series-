function name(num=10) {
    console.log(num);
}

name();
name(20);

//In above function the default parameter num=10, if we don't pass any value then it will print default value
//if we pass any values then it will print passing value